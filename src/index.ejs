<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>lgame-of-life</title>
  </head>
  <body>
    <script>
      // Define global early for libraries that expect Node's global
      if (typeof window !== 'undefined' && typeof window.global === 'undefined') {
        window.global = window
      }
    </script>
    <div id="app"></div>
    <!-- No Node APIs in renderer; avoid require/process here -->

    <!-- webpack builds are automatically injected -->
  </body>
</html>
